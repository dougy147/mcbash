#!/bin/sh

locate_destination_folder() {
    if [[ ! -d $destination ]]; then 
        mkdir -p "$destination" &> /dev/null || display_cannot_create_saving_dir
    fi
    if [[ ! -d $destination/checkpoints ]]; then 
        mkdir -p "$destination/checkpoints" || display_cannot_create_checkpoints_dir
    fi
}
